#!/bin/sh
set -e

PACKAGE=ragifix-mcp

case "$1" in
  purge)
    rm -rf "/etc/$PACKAGE" "/var/lib/$PACKAGE"
    if getent passwd "$PACKAGE" >/dev/null 2>&1; then
        deluser --system "$PACKAGE" >/dev/null 2>&1 || true
    fi
    ;;
esac

#DEBHELPER#

exit 0
